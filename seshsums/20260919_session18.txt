Session: 20260919 session 18

Work done:
- shiptrack/shipat: added required 4th arg <speed> (numeric), new "speed"
  column after lon. Existing ship-tracks.tsv NOT modified; user will repair
  old rows by hand.
- Remora/lamp_analysis.py: input is now a single .tar.gz (unpacked to a temp
  dir, plain files only, path-traversal guarded); plotting removed (matplotlib
  no longer imported, PDF plot section dropped, sections renumbered); the
  per-jump table removed from the PDF (only the count remains); pipeline
  factored into prepare() for reuse.
- Remora/lamp_plot.py (new): takes the same .tar.gz, interactive matplotlib
  window (methane w/ ON/OFF shading + windspeed panel, wheel zoom, arrow-key
  scroll, h = home, gaps between days not bridged).
- Tested on a synthetic 3-day tarball (not real data); interactive window
  itself only checked via Agg render.

Committed and pushed.

Later in session:
- lamp_analysis.py now reports every statistic twice: A = all data, B = with
  long ON/OFF stretches (> --long-factor (default 3) x median cycle length)
  removed; cycles never paired across a removed stretch. Reason: lamp state
  in long stretches is inferred from control signals and may be wrong (reboot
  silently turns lamps off).
- lamp_plot.py hatches the long stretches.
- Real data (lclogs_202609190503.tar.gz): 2 long stretches (21141 samples, 48%).
  Sample-level ON-OFF diff -0.0168 ppm (p~3e-25) with them, +0.0009 (p=0.71)
  without; paired and regression effects null either way. Windspeed in that
  data looks stuck/faulty (1e-5 scale, one spike) - not investigated.
- PDF text layout not visually verified (no PDF text tool available).

2026-09-23:
- New Remora/lamp_ramp_plot.py: overlays every ramp-up transient (the
  samples lamp_analysis.py/lamp_plot.py exclude as neither ON nor OFF,
  quad-log rigs only) on a common relative-time-from-ramp-start axis,
  methane baseline-subtracted per instance, mean +/- SEM error bars;
  second panel overlays the quads-on fraction itself as a sanity check
  the instances actually line up. --direction up|down|both (default up;
  on the real data ramp-down has 0 instances - the lamps switch off
  instantly, only power-up is staged). Refactored
  lamp_analysis.lamp_state_from_quads to share a new quad_on_counts()
  helper (identical behavior, verified) so lamp_ramp_plot.py doesn't
  duplicate the quad-event-scanning loop.
- Tested on lclogs_202609190503.tar.gz: 191 ramp-up instances, median
  98s; quads-on-fraction overlay confirms consistent staging; mean
  methane stays ~flat (no visible ramp-up effect) until the sparsely-
  covered tail. Confirmed the synthetic (no-quad-events) tarball and
  --direction down give clean error messages rather than crashing.
- Committed and pushed.

Later same day (2026-09-23):
- User reported the ramp-plot x-axis showed numbers in the thousands, asked
  if units were ms. Investigated: seconds were correct; the real cause was
  one outlier "ramp" block per tarball running ~130,191s (~36h/2170min),
  almost certainly a quad stuck partially on after a silent reboot, which
  stretched the shared time grid out to the length of that one instance and
  squeezed the real ~90-100s ramps into a sliver near zero.
- lamp_ramp_plot.py: added split_outliers() -- blocks longer than
  --outlier-factor (default 5x) the MEDIAN duration are excluded from the
  overlay and reported separately (count + durations) rather than plotted.
  Also dropped the quads-on-fraction panel per user request ("we only need
  the methane data") -- single-panel plot now.
- Verified on lclogs_202609190503.tar.gz: 190 instances overlaid (1 outlier,
  2170 min, excluded and reported), axis back to a normal 0-100s range.
- Committed and pushed.
